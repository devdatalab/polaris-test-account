# .bash_profile
# ONLY ADD to the bottom of this file

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

PATH=$PATH:$HOME/bin
export PATH

# User specific environment and startup programs
# Put your own module loads in ~/.modules (preferred), or here.
