# World-readable test account setup repo

This is a public repository. It is public so it can be easily cloned into a bare-bones RC user directory without any github credentials.

## What the test account is for

Everything in this directory is to configure a Polaris test account that is used almost exclusively for paper replication repository testing. The configuration and setup for the test account are duplicated in this folder, and there is a setup script that will return the test account back to its minimal initialization state, deleting everything that is not absolutely required. 

You should not be editing anything in here unless you know what you're doing. 
